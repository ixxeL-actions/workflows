---
name: Check GH actions templates
on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - "main"
jobs:
  check-gha-templates:
    uses: ixxeL-actions/workflows/.github/workflows/__check-gha-templates__.yaml@v2.0.2
