---
name: Auto tag workflows
on:
  push:
    branches:
      - main
    paths:
      - .github/**
jobs:
  auto-tag-repo:
    uses: ixxeL-actions/workflows/.github/workflows/__auto-tag__.yaml@v2.0.3
    with:
      git-workdir: .github
    secrets:
      git-token: ${{ secrets.PAT }}
